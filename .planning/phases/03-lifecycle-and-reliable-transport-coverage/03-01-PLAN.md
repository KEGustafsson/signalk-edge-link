---
phase: 03-lifecycle-and-reliable-transport-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/instance.test.js
  - __tests__/config-watcher.test.js
  - src/instance.ts
  - src/config-watcher.ts
autonomous: true
requirements:
  - V1-TEST-001
must_haves:
  truths:
    - "Phase 3 is test-first hardening: tests are the primary deliverable and source edits stay narrow."
    - "Decision coverage: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-14, D-15, D-16, D-17, D-18, and D-19."
    - "Stopped instances must not leak timers, watcher callbacks, socket recovery callbacks, subscription retries, heartbeat handles, or v2 periodic workers."
    - "Watcher recovery must be deterministic under fake timers and must stop recreating watchers after `close()` or `state.stopped`."
    - "No real Signal K server or real UDP network dependency is introduced for lifecycle regression coverage."
  artifacts:
    - path: __tests__/instance.test.js
      provides: "Instance lifecycle cleanup and client socket recovery regression coverage"
      contains: "socket recovery"
    - path: __tests__/config-watcher.test.js
      provides: "Watcher recovery and close cleanup regression coverage"
      contains: "createWatcherWithRecovery"
  key_links:
    - from: .planning/phases/03-lifecycle-and-reliable-transport-coverage/03-CONTEXT.md
      to: __tests__/instance.test.js
      via: "Decisions D-04 through D-07 require lifecycle cleanup coverage"
      pattern: "stop"
    - from: docs/code-quality-report.md
      to: src/instance.ts
      via: "Coverage gap called out for stop during reload, socket recovery, and lifecycle state transitions"
      pattern: "socket recovery"
---

<objective>
Add targeted lifecycle cleanup regression coverage for instance runtime and config watcher recovery.

Purpose: Prove rare stop/recovery/timer/watcher paths do not leak resources or mutate stopped instances.
Output: Focused Jest tests in `__tests__/instance.test.js` and `__tests__/config-watcher.test.js`, plus only narrow source fixes if the new tests expose current behavior drift.
</objective>

<execution_context>
@C:/Users/karl-/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/karl-/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lifecycle-and-reliable-transport-coverage/03-CONTEXT.md
@.planning/phases/03-lifecycle-and-reliable-transport-coverage/03-RESEARCH.md
@.planning/phases/03-lifecycle-and-reliable-transport-coverage/03-VALIDATION.md
@.planning/phases/03-lifecycle-and-reliable-transport-coverage/03-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@docs/code-quality-report.md
@src/instance.ts
@src/config-watcher.ts
@__tests__/instance.test.js
@__tests__/config-watcher.test.js
</context>

<threat_model>

## Threat Model

| ID     | Threat                                                                                         | Severity | Mitigation                                                                                                                        |
| ------ | ---------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-3-01 | Stopped instance callbacks mutate runtime state after `stop()`                                 | high     | Add fake-timer tests that trigger pending recovery/retry/debounce work after stop and assert no restart or state mutation occurs. |
| T-3-02 | Watcher recovery timers recreate watchers after the watcher handle is closed                   | high     | Add `createWatcherWithRecovery` tests for rename/error recovery followed by `close()` and timer advancement.                      |
| T-3-03 | Client socket recovery accumulates duplicate listeners or restarts periodic workers after stop | high     | Mock sockets and assert recovery cancellation, listener counts, and worker stop/start hooks.                                      |
| T-3-04 | Tests introduce real network/filesystem flakiness                                              | medium   | Use fake sockets/watchers and fake timers; reserve real filesystem use for existing storage tests only.                           |

Block condition: any high-severity threat without implemented regression coverage blocks plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add config watcher recovery and close cleanup tests</name>
  <files>__tests__/config-watcher.test.js, src/config-watcher.ts</files>
  <read_first>__tests__/config-watcher.test.js, src/config-watcher.ts, .planning/phases/03-lifecycle-and-reliable-transport-coverage/03-PATTERNS.md</read_first>
  <action>Add focused Jest tests for `createWatcherWithRecovery()` that cover: a `rename` event schedules watcher recreation after `WATCHER_RECOVERY_DELAY`; calling the returned `close()` before the timer fires prevents recreation; an `error` event closes the current watcher and schedules recreation; setting `state.stopped = true` before the recovery timer fires prevents recreation and suppresses extra watcher creation. Use a mocked `fs.watch` that returns watcher handles with `on()` and `close()` methods. Restore real timers in `afterEach`. If the tests expose a source bug, make the smallest fix in `src/config-watcher.ts` without changing public function signatures.</action>
  <verify>Run `npm test -- --runTestsByPath __tests__/config-watcher.test.js` and `rg -n "rename|recovery|close\\(\\)" __tests__/config-watcher.test.js src/config-watcher.ts`.</verify>
  <acceptance_criteria>
    - `__tests__/config-watcher.test.js` contains a test whose name includes `does not recreate watcher after close`.
    - `__tests__/config-watcher.test.js` contains a test whose name includes `does not recreate watcher when stopped`.
    - The focused config watcher suite passes with fake timers restored after the tests.
    - No production dependency or new test framework is added.
  </acceptance_criteria>
  <done>Watcher recovery cannot recreate closed or stopped watchers.</done>
</task>

<task type="auto">
  <name>Task 2: Add instance stop and socket recovery cancellation tests</name>
  <files>__tests__/instance.test.js, src/instance.ts</files>
  <read_first>__tests__/instance.test.js, src/instance.ts, src/pipeline-v2-client.ts, .planning/phases/03-lifecycle-and-reliable-transport-coverage/03-CONTEXT.md</read_first>
  <action>Add focused Jest tests for client-mode `createInstance()` lifecycle cleanup. Mock `dgram.createSocket()` with controllable socket handles so a socket `error` event schedules recovery. Cover: `stop()` before the recovery timer fires clears `state.socketRecoveryTimer`, sets `state.socketRecoveryInProgress` false, and does not create a replacement socket after timers advance; repeated socket recovery does not accumulate duplicate `message` listeners on the recovered socket; `stop()` cancels `subscriptionRetryTimer`, `pendingRetry`, metadata timers, source snapshot timer, config debounce timers, and heartbeat handle when those handles exist in state. If private state setup requires a narrow helper, prefer a local test helper in the test file before editing source.</action>
  <verify>Run `npm test -- --runTestsByPath __tests__/instance.test.js` and `rg -n "socket recovery|subscriptionRetryTimer|sourceSnapshotTimer|heartbeatHandle|configDebounceTimers" __tests__/instance.test.js src/instance.ts`.</verify>
  <acceptance_criteria>
    - `__tests__/instance.test.js` contains a test whose name includes `cancels pending socket recovery on stop`.
    - `__tests__/instance.test.js` contains a test whose name includes `does not duplicate control packet listeners after recovery`.
    - `__tests__/instance.test.js` contains assertions for at least three timer or worker cleanup fields from `subscriptionRetryTimer`, `socketRecoveryTimer`, `sourceSnapshotTimer`, `metaTimer`, `configDebounceTimers`, `heartbeatHandle`, or v2 worker stop hooks.
    - The focused instance suite passes.
  </acceptance_criteria>
  <done>Instance stop/recovery cleanup has deterministic regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run lifecycle static and focused validation</name>
  <files>__tests__/instance.test.js, __tests__/config-watcher.test.js, src/instance.ts, src/config-watcher.ts</files>
  <read_first>package.json, tsconfig.json, __tests__/instance.test.js, __tests__/config-watcher.test.js</read_first>
  <action>Run the lifecycle-focused checks after tests and source edits settle: `npm test -- --runTestsByPath __tests__/instance.test.js __tests__/config-watcher.test.js`, `npm run check:ts`, and `npx prettier --check __tests__/instance.test.js __tests__/config-watcher.test.js src/instance.ts src/config-watcher.ts`. Fix only failures caused by this plan's changes.</action>
  <verify>Run the commands listed in the action.</verify>
  <acceptance_criteria>
    - Focused lifecycle Jest command exits 0.
    - `npm run check:ts` exits 0.
    - Prettier check exits 0 for touched lifecycle files.
  </acceptance_criteria>
  <done>Lifecycle coverage is validated and ready for the transport wave.</done>
</task>

</tasks>

<verification>

- [ ] `npm test -- --runTestsByPath __tests__/instance.test.js __tests__/config-watcher.test.js` passes.
- [ ] `npm run check:ts` passes.
- [ ] `rg -n "does not recreate watcher after close|does not recreate watcher when stopped|cancels pending socket recovery on stop|does not duplicate control packet listeners after recovery" __tests__/config-watcher.test.js __tests__/instance.test.js` shows the new regression tests.
- [ ] `npx prettier --check __tests__/instance.test.js __tests__/config-watcher.test.js src/instance.ts src/config-watcher.ts` passes.

</verification>

<success_criteria>

- V1-TEST-001 is covered by deterministic lifecycle cleanup tests.
- Watcher recovery and instance socket recovery have stop/cancel regression coverage.
- Any source changes are narrow, behavior-driven, and covered by the new tests.
- No new runtime dependency, test framework, protocol behavior, config field, UI surface, or docs churn is introduced.

</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-and-reliable-transport-coverage/03-01-SUMMARY.md`.
</output>
