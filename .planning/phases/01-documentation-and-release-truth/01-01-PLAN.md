---
phase: 01-documentation-and-release-truth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture-overview.md
  - docs/api-reference.md
autonomous: true
requirements:
  - V1-DOC-001
user_setup: []
must_haves:
  truths:
    - "Architecture docs reference current source files instead of legacy source filenames."
    - "API reference current-version marker matches package.json version."
    - "No runtime behavior, public API shape, or package metadata is changed by this plan."
  artifacts:
    - path: docs/architecture-overview.md
      provides: "Current architecture source-file map"
      contains: "src/bonding.ts"
    - path: docs/api-reference.md
      provides: "Current API release marker"
      contains: "current: 2.5.0"
  key_links:
    - from: package.json
      to: docs/api-reference.md
      via: "package version reflected in API reference title"
      pattern: "current: 2\\.5\\.0"
---

<objective>
Correct the known stale public documentation references.

Purpose: Contributors and operators should be able to trust the architecture and API reference docs before release checks enforce them.
Output: Updated architecture source-file references and API reference current-version marker.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-documentation-and-release-truth/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@package.json
@docs/architecture-overview.md
@docs/api-reference.md
@src/bonding.ts
@src/congestion.ts
@src/monitoring.ts
@src/sequence.ts
</context>

<threat_model>

## Threat Model

| ID     | Threat                                                | Severity | Mitigation                                                                                    |
| ------ | ----------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------- |
| T-1-01 | Stale architecture docs send maintainers to old files | low      | Replace legacy source filenames with actual current paths and verify with grep.               |
| T-1-02 | Stale API version marker undermines release trust     | low      | Use `package.json` version as source of truth and verify exact current-version marker.        |
| T-1-03 | Docs accidentally expose secret examples              | low      | Do not add real token, secret, or local environment values; keep existing redaction examples. |

Block condition: no high-severity threats are introduced by this docs-only plan.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy architecture source filenames</name>
  <files>docs/architecture-overview.md</files>
  <read_first>docs/architecture-overview.md, src/bonding.ts, src/congestion.ts, src/monitoring.ts, src/sequence.ts, .planning/codebase/CONCERNS.md</read_first>
  <action>Update the component map and key source files table in `docs/architecture-overview.md` so `congestion-control.ts` becomes `congestion.ts`, `bonding-manager.ts` becomes `bonding.ts`, `sequence-tracker.ts` becomes `sequence.ts`, and `alert-manager.ts` becomes `monitoring.ts` or a monitoring alerts label backed by `src/monitoring.ts`. Keep references to `docs/congestion-control.md` untouched because that is a real documentation file, not a stale source filename.</action>
  <verify>Run `rg -n "bonding-manager|congestion-control|alert-manager|sequence-tracker" docs/architecture-overview.md` and confirm it returns no matches; then run `rg -n "src/(bonding|congestion|monitoring|sequence)\\.ts|bonding\\.ts|congestion\\.ts|monitoring\\.ts|sequence\\.ts" docs/architecture-overview.md` and confirm all four current names appear.</verify>
  <acceptance_criteria>
    - `docs/architecture-overview.md` does not contain `bonding-manager.ts`.
    - `docs/architecture-overview.md` does not contain `congestion-control.ts`.
    - `docs/architecture-overview.md` does not contain `alert-manager.ts`.
    - `docs/architecture-overview.md` does not contain `sequence-tracker.ts`.
    - `docs/architecture-overview.md` contains `bonding.ts`, `congestion.ts`, `monitoring.ts`, and `sequence.ts`.
  </acceptance_criteria>
  <done>Architecture overview names the current source files and keeps documentation-file links intact.</done>
</task>

<task type="auto">
  <name>Task 2: Align API reference current version with package metadata</name>
  <files>docs/api-reference.md</files>
  <read_first>package.json, docs/api-reference.md</read_first>
  <action>Read `version` from `package.json` and update the `docs/api-reference.md` H1 current-version marker from `2.1.1` to that exact value, currently `2.5.0`. Do not change historical endpoint examples or behavior descriptions unless they are directly tied to the H1 current-version claim.</action>
  <verify>Run `node -e "const fs=require('fs');const p=require('./package.json');const d=fs.readFileSync('docs/api-reference.md','utf8');if(!d.includes('current: '+p.version))process.exit(1)"` and `rg -n "current: 2\\.1\\.1" docs/api-reference.md` expecting no matches.</verify>
  <acceptance_criteria>
    - `docs/api-reference.md` title contains `current: 2.5.0`.
    - `docs/api-reference.md` no longer contains `current: 2.1.1`.
    - `package.json` is not modified by this task.
  </acceptance_criteria>
  <done>API reference current-version marker matches package metadata.</done>
</task>

</tasks>

<verification>

- [ ] `rg -n "bonding-manager|congestion-control|alert-manager|sequence-tracker" docs/architecture-overview.md` returns no matches.
- [ ] `rg -n "current: 2\\.1\\.1" docs/api-reference.md` returns no matches.
- [ ] `node -e "const fs=require('fs');const p=require('./package.json');const d=fs.readFileSync('docs/api-reference.md','utf8');if(!d.includes('current: '+p.version))process.exit(1)"` exits 0.
- [ ] `npx prettier --check docs/architecture-overview.md docs/api-reference.md` passes.

</verification>

<success_criteria>

- All tasks completed.
- All verification checks pass.
- Phase requirement `V1-DOC-001` is satisfied for the known architecture and API doc drift called out in the codebase map.
- No runtime source files, package metadata, generated artifacts, or unrelated working-tree changes are modified.

</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-and-release-truth/01-01-SUMMARY.md`.
</output>
