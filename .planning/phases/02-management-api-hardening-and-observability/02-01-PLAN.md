---
phase: 02-management-api-hardening-and-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes.ts
  - src/routes/types.ts
  - __tests__/routes.auth-guard.test.js
  - __tests__/routes.rate-limit.test.js
autonomous: true
requirements:
  - V1-SEC-001
  - V1-SEC-002
  - V1-SEC-003
  - V1-OPS-002
user_setup: []
must_haves:
  truths:
    - "Context decisions D-01 through D-10 remain true: management auth behavior is unchanged and telemetry is additive."
    - "Every management auth decision records exactly one bounded counter event with decision, reason, and action."
    - "Telemetry labels and JSON fields never include tokens, IP addresses, user agents, raw paths, or request parameters."
    - "JSON `/status` and `/metrics` expose a top-level `managementAuth` block without removing or renaming existing fields."
  artifacts:
    - path: src/routes.ts
      provides: "Route-owned management auth telemetry state and JSON exposure"
      contains: "managementAuth"
    - path: src/routes/types.ts
      provides: "Typed route context additions if route modules need telemetry snapshots"
      contains: "managementAuth"
    - path: __tests__/routes.auth-guard.test.js
      provides: "Compatibility and denied/allowed auth decision coverage"
      contains: "token_required_unconfigured"
    - path: __tests__/routes.rate-limit.test.js
      provides: "JSON telemetry and redaction regression coverage"
      contains: "managementAuth"
  key_links:
    - from: .planning/phases/02-management-api-hardening-and-observability/02-CONTEXT.md
      to: src/routes.ts
      via: "D-05 route-owned auth telemetry"
      pattern: "authorizeManagement"
    - from: src/routes.ts
      to: __tests__/routes.auth-guard.test.js
      via: "auth decision reasons covered by focused tests"
      pattern: "open_access|valid_token|missing_token|invalid_token|token_required_unconfigured"
---

<objective>
Add management auth telemetry core and JSON exposure.

Purpose: Operators should be able to see allowed and denied management auth decisions without changing management API behavior.
Output: Route-owned counters recorded from `authorizeManagement()` and exposed additively through `/status` and `/metrics`.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-management-api-hardening-and-observability/02-CONTEXT.md
@.planning/phases/02-management-api-hardening-and-observability/02-RESEARCH.md
@.planning/phases/02-management-api-hardening-and-observability/02-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@src/routes.ts
@src/routes/types.ts
@__tests__/routes.auth-guard.test.js
@__tests__/routes.rate-limit.test.js
</context>

<threat_model>

## Threat Model

| ID     | Threat                                                           | Severity | Mitigation                                                                                                                 |
| ------ | ---------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-2-01 | New telemetry leaks sensitive or high-cardinality request data   | high     | Store only bounded `decision`, `reason`, and existing action strings; add no token/IP/user-agent/path labels.              |
| T-2-02 | Instrumentation changes fail-open/fail-closed compatibility      | high     | Preserve existing responses and add tests for open access, required-unconfigured, missing, invalid, and valid token paths. |
| T-2-03 | Counters are tied to one runtime instance instead of route state | medium   | Keep telemetry state in `src/routes.ts` and expose snapshots without mutating per-instance metrics.                        |

Block condition: any high-severity threat without implemented mitigation blocks completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add route-owned auth decision telemetry</name>
  <files>src/routes.ts</files>
  <read_first>src/routes.ts, .planning/phases/02-management-api-hardening-and-observability/02-CONTEXT.md, __tests__/routes.auth-guard.test.js</read_first>
  <action>Add a route-owned management auth telemetry structure in `src/routes.ts`. Record exactly one event for each `authorizeManagement(req, res, action)` decision with `decision` set to `allowed` or `denied`, `reason` set to one of `open_access`, `valid_token`, `missing_token`, `invalid_token`, or `token_required_unconfigured`, and `action` set to the existing bounded action string passed by the route. Preserve all current status codes, response bodies, token sources, and auth defaults. Do not store token values, IP addresses, user agents, raw URLs, or request paths.</action>
  <verify>Run `npm test -- __tests__/routes.auth-guard.test.js __tests__/routes.rate-limit.test.js` and inspect failures for behavior changes. Run `rg -n "open_access|valid_token|missing_token|invalid_token|token_required_unconfigured" src/routes.ts __tests__/routes.auth-guard.test.js __tests__/routes.rate-limit.test.js`.</verify>
  <acceptance_criteria>
    - Open management access without a configured token still succeeds when token auth is not required.
    - Required auth with no configured token still returns the existing `403` guidance response.
    - Missing token with a configured expected token records `denied/missing_token`.
    - Invalid token records `denied/invalid_token`.
    - Valid token records `allowed/valid_token`.
    - Open access records `allowed/open_access`.
  </acceptance_criteria>
  <done>Auth decisions are counted centrally without changing auth behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Expose management auth telemetry in JSON responses</name>
  <files>src/routes.ts, src/routes/types.ts, __tests__/routes.rate-limit.test.js</files>
  <read_first>src/routes.ts, src/routes/types.ts, __tests__/routes.rate-limit.test.js, __tests__/routes.metrics.test.js</read_first>
  <action>Expose a snapshot of the route-owned telemetry through a top-level `managementAuth` block in `/status` and `/metrics`. Keep the response additive: do not remove, rename, or relocate existing fields. If route modules need access to the snapshot, add the narrowest `RouteContext` type addition. Include totals and enough bounded breakdown data to support operator troubleshooting. Add tests that prove the JSON shape exists and does not contain configured token values, transport secrets, IP addresses, or user-agent strings.</action>
  <verify>Run `npm test -- __tests__/routes.rate-limit.test.js` and `npm run check:ts`. Run `rg -n "managementAuth" src/routes.ts src/routes/types.ts __tests__/routes.rate-limit.test.js`.</verify>
  <acceptance_criteria>
    - `/status` includes a top-level `managementAuth` block.
    - `/metrics` includes a top-level `managementAuth` block.
    - Existing status and metrics response fields are preserved.
    - Telemetry JSON contains only bounded decisions, reasons, actions, and counts.
    - Tests prove no token or secret-like configured values are exposed by the new block.
  </acceptance_criteria>
  <done>Management auth telemetry is visible in JSON management surfaces.</done>
</task>

</tasks>

<verification>

- [ ] `npm test -- __tests__/routes.auth-guard.test.js __tests__/routes.rate-limit.test.js` passes.
- [ ] `npm run check:ts` passes.
- [ ] `rg -n "open_access|valid_token|missing_token|invalid_token|token_required_unconfigured" src/routes.ts __tests__/routes.auth-guard.test.js __tests__/routes.rate-limit.test.js` shows implementation and coverage.
- [ ] `rg -n "managementAuth" src/routes.ts src/routes/types.ts __tests__/routes.rate-limit.test.js` shows JSON exposure and tests.
- [ ] `npx prettier --check src/routes.ts src/routes/types.ts __tests__/routes.auth-guard.test.js __tests__/routes.rate-limit.test.js` passes.

</verification>

<success_criteria>

- All tasks completed.
- All verification checks pass.
- Phase requirements `V1-SEC-001`, `V1-SEC-002`, `V1-SEC-003`, and `V1-OPS-002` are advanced without behavior-breaking auth changes.
- New telemetry is route-owned, additive, and low-cardinality.
- No unrelated working-tree changes, package-lock changes, generated tarballs, or source refactors are committed.

</success_criteria>

<output>
After completion, create `.planning/phases/02-management-api-hardening-and-observability/02-01-SUMMARY.md`.
</output>
