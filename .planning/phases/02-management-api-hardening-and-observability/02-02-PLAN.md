---
phase: 02-management-api-hardening-and-observability
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/routes/metrics.ts
  - src/prometheus.ts
  - __tests__/routes.metrics.test.js
  - __tests__/v2/prometheus.test.js
  - docs/api-reference.md
  - docs/metrics.md
  - docs/management-tools.md
  - docs/security.md
autonomous: true
requirements:
  - V1-SEC-002
  - V1-SEC-003
  - V1-OPS-002
user_setup: []
must_haves:
  truths:
    - "Context decisions D-10 through D-13 and D-19 through D-21 remain true: metrics exposure is additive, global, documented, and tested."
    - "Prometheus emits `signalk_edge_link_management_auth_requests_total` once per scrape, not once per connection instance."
    - "Prometheus labels are exactly bounded management auth dimensions such as `decision`, `reason`, and `action`."
    - "Docs describe the JSON and Prometheus auth telemetry without token, secret, IP, or user-agent examples."
  artifacts:
    - path: src/routes/metrics.ts
      provides: "Global management auth Prometheus emission"
      contains: "management_auth_requests_total"
    - path: src/prometheus.ts
      provides: "Reusable formatting helper if needed"
      contains: "management_auth_requests_total"
    - path: __tests__/routes.metrics.test.js
      provides: "Metrics route coverage for JSON/Prometheus auth counters"
      contains: "management_auth_requests_total"
    - path: __tests__/v2/prometheus.test.js
      provides: "Formatter-level bounded label coverage"
      contains: "management_auth_requests_total"
    - path: docs/metrics.md
      provides: "Operator-facing metric interpretation"
      contains: "signalk_edge_link_management_auth_requests_total"
  key_links:
    - from: src/routes.ts
      to: src/routes/metrics.ts
      via: "Route context exposes route-owned management auth telemetry snapshot"
      pattern: "managementAuth"
    - from: src/routes/metrics.ts
      to: docs/metrics.md
      via: "Prometheus counter documented with bounded labels"
      pattern: "signalk_edge_link_management_auth_requests_total"
---

<objective>
Expose management auth telemetry through Prometheus and operator docs.

Purpose: Operators should be able to dashboard allowed and denied management requests without scraping per-instance duplicates or sensitive labels.
Output: A global Prometheus auth counter, focused tests, and docs aligned with the new JSON/Prometheus surfaces.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-management-api-hardening-and-observability/02-CONTEXT.md
@.planning/phases/02-management-api-hardening-and-observability/02-RESEARCH.md
@.planning/phases/02-management-api-hardening-and-observability/02-VALIDATION.md
@.planning/phases/02-management-api-hardening-and-observability/02-01-SUMMARY.md
@src/routes.ts
@src/routes/types.ts
@src/routes/metrics.ts
@src/prometheus.ts
@__tests__/routes.metrics.test.js
@__tests__/v2/prometheus.test.js
@docs/api-reference.md
@docs/metrics.md
@docs/management-tools.md
@docs/security.md
</context>

<threat_model>

## Threat Model

| ID     | Threat                                                        | Severity | Mitigation                                                                                                  |
| ------ | ------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------- |
| T-2-04 | Prometheus counter is duplicated once per connection bundle   | high     | Format management auth counters once outside the per-bundle Prometheus loop and test with multiple bundles. |
| T-2-05 | Docs or metrics examples leak secrets or high-cardinality IDs | high     | Keep examples limited to bounded labels and scrub token/IP/user-agent/path examples from new sections.      |
| T-2-06 | Metric labels become incompatible with Prometheus conventions | medium   | Reuse existing label escaping/sanitizing helpers and add formatter tests.                                   |

Block condition: any high-severity threat without implemented mitigation blocks completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add global Prometheus auth counters</name>
  <files>src/routes/metrics.ts, src/prometheus.ts, __tests__/routes.metrics.test.js, __tests__/v2/prometheus.test.js</files>
  <read_first>src/routes/metrics.ts, src/prometheus.ts, src/routes/types.ts, __tests__/routes.metrics.test.js, __tests__/v2/prometheus.test.js</read_first>
  <action>Expose the route-owned management auth counters in `/prometheus` as `signalk_edge_link_management_auth_requests_total{decision,reason,action}`. Place the formatting so counters are emitted once per scrape, before or after the existing per-instance metric blocks, not inside the per-bundle loop. Reuse existing Prometheus helper patterns for HELP/TYPE lines, label escaping, and numeric counts. Add tests for at least one allowed and one denied sample, bounded labels, and a multiple-bundle scrape that proves the global counter is not duplicated per connection.</action>
  <verify>Run `npm test -- __tests__/routes.metrics.test.js __tests__/v2/prometheus.test.js` and `npm run check:ts`. Run `rg -n "signalk_edge_link_management_auth_requests_total|management_auth_requests_total" src/routes/metrics.ts src/prometheus.ts __tests__/routes.metrics.test.js __tests__/v2/prometheus.test.js`.</verify>
  <acceptance_criteria>
    - `/prometheus` includes `# HELP` and `# TYPE` lines for the management auth counter.
    - Counter samples include labels for `decision`, `reason`, and `action`.
    - The counter is emitted once per scrape, regardless of connection count.
    - Label values come from bounded decisions, reasons, and action strings.
  </acceptance_criteria>
  <done>Management auth telemetry is available to Prometheus without per-instance duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Document management auth telemetry surfaces</name>
  <files>docs/api-reference.md, docs/metrics.md, docs/management-tools.md, docs/security.md</files>
  <read_first>docs/api-reference.md, docs/metrics.md, docs/management-tools.md, docs/security.md, .planning/phases/02-management-api-hardening-and-observability/02-CONTEXT.md</read_first>
  <action>Update operator docs to describe the new additive `managementAuth` JSON block in `/status` and `/metrics`, the Prometheus counter `signalk_edge_link_management_auth_requests_total`, the bounded label meanings, and the preserved management auth defaults. Keep security docs explicit that production deployments should configure a management token and trusted network controls. Do not add real-looking tokens, transport secrets, IP addresses, user agents, or parameterized paths to examples.</action>
  <verify>Run `rg -n "managementAuth|signalk_edge_link_management_auth_requests_total|token_required_unconfigured|open_access" docs/api-reference.md docs/metrics.md docs/management-tools.md docs/security.md` and `npx prettier --check docs/api-reference.md docs/metrics.md docs/management-tools.md docs/security.md`.</verify>
  <acceptance_criteria>
    - API docs describe the `managementAuth` response block.
    - Metrics docs describe the Prometheus counter and labels.
    - Management tools docs mention token-compatible behavior and telemetry interpretation.
    - Security docs preserve fail-open compatibility notes and fail-closed production guidance.
    - New docs contain no real token, secret, IP address, or user-agent examples.
  </acceptance_criteria>
  <done>Operator docs match the implemented metrics surfaces.</done>
</task>

</tasks>

<verification>

- [x] `npm test -- __tests__/routes.metrics.test.js __tests__/v2/prometheus.test.js` passes.
- [x] `npm run check:ts` passes.
- [x] `rg -n "signalk_edge_link_management_auth_requests_total|management_auth_requests_total" src/routes/metrics.ts src/prometheus.ts __tests__/routes.metrics.test.js __tests__/v2/prometheus.test.js` shows implementation and tests.
- [x] `rg -n "managementAuth|signalk_edge_link_management_auth_requests_total|token_required_unconfigured|open_access" docs/api-reference.md docs/metrics.md docs/management-tools.md docs/security.md` shows docs coverage.
- [x] `npx prettier --check src/routes/metrics.ts src/prometheus.ts __tests__/routes.metrics.test.js __tests__/v2/prometheus.test.js docs/api-reference.md docs/metrics.md docs/management-tools.md docs/security.md` passes.

</verification>

<success_criteria>

- All tasks completed.
- All verification checks pass.
- Phase requirements `V1-SEC-002`, `V1-SEC-003`, and `V1-OPS-002` are satisfied for the Prometheus and docs surfaces.
- Prometheus output is global, additive, bounded, and secret-free.
- No unrelated working-tree changes, package-lock changes, generated tarballs, or source refactors are committed.

</success_criteria>

<output>
After completion, create `.planning/phases/02-management-api-hardening-and-observability/02-02-SUMMARY.md`.
</output>
