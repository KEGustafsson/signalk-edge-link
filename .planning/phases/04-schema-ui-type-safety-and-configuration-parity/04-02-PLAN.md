---
phase: 04-schema-ui-type-safety-and-configuration-parity
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/shared/connection-schema.ts
  - src/connection-config.ts
  - src/types.ts
  - src/routes/config.ts
  - src/routes/connections.ts
  - __tests__/schema-compat.test.js
  - __tests__/connection-config.test.js
  - __tests__/routes.config-validation.test.js
autonomous: true
requirements:
  - V1-UI-002
user_setup: []
must_haves:
  truths:
    - "Phase 4 keeps `src/shared/connection-schema.ts` as the shared schema source of truth."
    - "Decision coverage: D-05, D-06, D-07, D-08, D-09, D-15, D-16, and D-17."
    - "`udpMetaPort` is runtime-supported for v1 metadata transport and must become an optional public connection field across schema, validation, sanitization, routes, and tests."
    - "`managementApiToken` is plugin-level secret configuration, not a per-connection public field."
    - "No token, transport secret, IP address, user agent, raw path, or request parameter may be introduced into docs examples, logs, telemetry labels, or per-connection payloads."
  artifacts:
    - path: src/shared/connection-schema.ts
      provides: "Shared `udpMetaPort` schema property"
      contains: "udpMetaPort"
    - path: src/connection-config.ts
      provides: "Runtime validation/sanitization parity"
      contains: "VALID_CONNECTION_KEYS"
    - path: __tests__/connection-config.test.js
      provides: "Runtime config parity regression coverage"
      contains: "udpMetaPort"
  key_links:
    - from: .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-CONTEXT.md
      to: src/connection-config.ts
      via: "D-06 and D-07 require runtime parity for `udpMetaPort`"
      pattern: "VALID_CONNECTION_KEYS"
    - from: docs/configuration-reference.md
      to: src/shared/connection-schema.ts
      via: "Docs already describe v1 `udpMetaPort`; schema must expose it"
      pattern: "udpMetaPort"
---

<objective>
Reconcile shared schema, runtime validation, and route parity for public connection fields.

Purpose: Make the runtime-supported `udpMetaPort` connection field visible and validated across code surfaces, while keeping `managementApiToken` plugin-level only.
Output: Shared schema, validation/sanitization, route allowlists, and focused tests agree on the same connection-field contract.
</objective>

<execution_context>
@C:/Users/karl-/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/karl-/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-CONTEXT.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-RESEARCH.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-VALIDATION.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-PATTERNS.md
@src/shared/connection-schema.ts
@src/connection-config.ts
@src/types.ts
@src/index.ts
@src/routes/config.ts
@src/routes/connections.ts
@__tests__/schema-compat.test.js
@__tests__/connection-config.test.js
@__tests__/routes.config-validation.test.js
</context>

<threat_model>

## Threat Model

| ID     | Threat                                                                                                            | Severity | Mitigation                                                                                                          |
| ------ | ----------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------- |
| T-4-04 | Runtime-supported `udpMetaPort` remains dropped by sanitization or unavailable in the UI/schema                   | high     | Add `udpMetaPort` to shared schema, runtime allowed keys, validation, sanitization tests, and route mutable fields. |
| T-4-05 | `managementApiToken` is accidentally treated as per-connection config and leaks through saved connection payloads | high     | Remove per-connection typing for `managementApiToken` and add tests that sanitize drops it from connection objects. |
| T-4-06 | Validation rejects existing configs that omit `udpMetaPort` or use v2/v3 without metadata                         | high     | Make `udpMetaPort` optional; validate only when present; preserve omission as valid.                                |
| T-4-07 | Route PATCH behavior diverges from plugin-config save behavior                                                    | medium   | Add route validation/allowlist tests for `udpMetaPort` and compare legacy/per-connection validation payloads.       |

Block condition: any high-severity threat without tests and focused validation blocks plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add shared schema coverage for udpMetaPort</name>
  <files>src/shared/connection-schema.ts, __tests__/schema-compat.test.js</files>
  <read_first>src/shared/connection-schema.ts, src/types.ts, docs/configuration-reference.md, __tests__/schema-compat.test.js, .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-PATTERNS.md</read_first>
  <action>Add an optional schema property for `udpMetaPort` with `type: "integer"`, `title: "v1 Metadata UDP Port"`, `description` containing `Optional separate UDP port for v1 metadata packets; ignored by v2/v3.`, `minimum: 1024`, and `maximum: 65535`. Include it in the backend connection item schema and the webapp connection schema. Add schema tests that import `buildConnectionItemSchema()` and `buildWebappConnectionSchema()` and assert both schema surfaces contain `properties.udpMetaPort`, `minimum === 1024`, and `maximum === 65535`.</action>
  <verify>Run `npm.cmd test -- --runTestsByPath __tests__/schema-compat.test.js` and `rg -n "udpMetaPort|v1 Metadata UDP Port|minimum: 1024|maximum: 65535" src/shared/connection-schema.ts __tests__/schema-compat.test.js`.</verify>
  <acceptance_criteria>
    - `src/shared/connection-schema.ts` contains `udpMetaPort`.
    - `src/shared/connection-schema.ts` contains `v1 Metadata UDP Port`.
    - `__tests__/schema-compat.test.js` imports or requires `buildConnectionItemSchema`.
    - `__tests__/schema-compat.test.js` imports or requires `buildWebappConnectionSchema`.
    - The focused schema compatibility suite exits 0.
  </acceptance_criteria>
  <done>Both backend and webapp schema builders expose the runtime-supported v1 metadata port field.</done>
</task>

<task type="auto">
  <name>Task 2: Align ConnectionConfig and runtime sanitization</name>
  <files>src/types.ts, src/connection-config.ts, __tests__/connection-config.test.js</files>
  <read_first>src/types.ts, src/connection-config.ts, __tests__/connection-config.test.js, src/instance.ts, src/pipeline.ts</read_first>
  <action>Add `"udpMetaPort"` to `VALID_CONNECTION_KEYS` in `src/connection-config.ts`. In `validateConnectionConfig()`, if `conn.udpMetaPort !== undefined`, require it to be an integer between 1024 and 65535 and return the exact error text `${p}udpMetaPort must be an integer between 1024 and 65535` when invalid. Do not require `udpMetaPort` for any protocol. Remove `managementApiToken?: string` from the per-connection `ConnectionConfig` interface in `src/types.ts`; keep plugin-level management token typing under `PluginRef._currentOptions`. Add tests in `__tests__/connection-config.test.js` proving valid client and server configs accept `udpMetaPort`, invalid `udpMetaPort: 100` is rejected with the exact error, `sanitizeConnectionConfig()` preserves `udpMetaPort`, and `sanitizeConnectionConfig()` drops a per-connection `managementApiToken` value.</action>
  <verify>Run `npm.cmd test -- --runTestsByPath __tests__/connection-config.test.js` and `npm.cmd run check:ts`. Run `rg -n "\"udpMetaPort\"|udpMetaPort must be an integer|managementApiToken" src/connection-config.ts src/types.ts __tests__/connection-config.test.js`.</verify>
  <acceptance_criteria>
    - `src/connection-config.ts` includes `"udpMetaPort"` in `VALID_CONNECTION_KEYS`.
    - `src/connection-config.ts` contains the error string `udpMetaPort must be an integer between 1024 and 65535`.
    - `src/types.ts` no longer declares `managementApiToken?: string` inside `ConnectionConfig`.
    - `__tests__/connection-config.test.js` proves sanitize preserves `udpMetaPort`.
    - `__tests__/connection-config.test.js` proves sanitize drops per-connection `managementApiToken`.
    - Focused connection-config tests and backend typecheck exit 0.
  </acceptance_criteria>
  <done>Runtime validation and types agree on public versus plugin-level configuration fields.</done>
</task>

<task type="auto">
  <name>Task 3: Align config routes and route validation coverage</name>
  <files>src/routes/config.ts, src/routes/connections.ts, __tests__/routes.config-validation.test.js</files>
  <read_first>src/routes/config.ts, src/routes/connections.ts, src/connection-config.ts, __tests__/routes.config-validation.test.js, docs/api-reference.md</read_first>
  <action>Ensure plugin-config save in `src/routes/config.ts` continues to pass all connection entries through `sanitizeConnectionConfig()` after `udpMetaPort` is added. Add `"udpMetaPort"` to the connection PATCH mutable-field allowlist in `src/routes/connections.ts` so per-connection PATCH can update the same public field as plugin-config save. Add route validation tests proving legacy plugin-config and per-connection config validation accept `udpMetaPort: 5001` and reject `udpMetaPort: 100` with the same `udpMetaPort must be an integer between 1024 and 65535` error body where those routes validate connection payloads.</action>
  <verify>Run `npm.cmd test -- --runTestsByPath __tests__/routes.config-validation.test.js` and `rg -n "\"udpMetaPort\"|udpMetaPort must be an integer" src/routes/config.ts src/routes/connections.ts __tests__/routes.config-validation.test.js`.</verify>
  <acceptance_criteria>
    - `src/routes/connections.ts` includes `"udpMetaPort"` in the mutable field allowlist near `protocolVersion`, `testAddress`, and `testPort`.
    - `__tests__/routes.config-validation.test.js` includes a positive route validation case for `udpMetaPort: 5001`.
    - `__tests__/routes.config-validation.test.js` includes a negative route validation case for `udpMetaPort: 100`.
    - The focused route validation suite exits 0.
  </acceptance_criteria>
  <done>REST route behavior accepts and rejects `udpMetaPort` consistently with runtime config validation.</done>
</task>

</tasks>

<verification>

- [ ] `npm.cmd test -- --runTestsByPath __tests__/schema-compat.test.js __tests__/connection-config.test.js __tests__/routes.config-validation.test.js` passes.
- [ ] `npm.cmd run check:ts` passes.
- [ ] `rg -n "udpMetaPort|v1 Metadata UDP Port" src/shared/connection-schema.ts src/connection-config.ts src/routes/connections.ts __tests__/schema-compat.test.js __tests__/connection-config.test.js __tests__/routes.config-validation.test.js` shows implementation and tests.
- [ ] `rg -n "managementApiToken\\?: string" src/types.ts` returns no per-connection `ConnectionConfig` match.
- [ ] `npx.cmd prettier --check src/shared/connection-schema.ts src/connection-config.ts src/types.ts src/routes/config.ts src/routes/connections.ts __tests__/schema-compat.test.js __tests__/connection-config.test.js __tests__/routes.config-validation.test.js` passes.

</verification>

<success_criteria>

- V1-UI-002 is advanced by schema, runtime validation, route, and test parity for the public `udpMetaPort` field.
- `managementApiToken` remains top-level plugin configuration and is not preserved as per-connection config.
- Omitted `udpMetaPort` remains valid for existing operator configs.
- No docs/sample updates are skipped when source truth changes; Plan 04-03 must complete the public artifact parity work.

</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-02-SUMMARY.md`.
</output>
