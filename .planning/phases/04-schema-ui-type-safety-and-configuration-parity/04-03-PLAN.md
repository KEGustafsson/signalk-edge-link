---
phase: 04-schema-ui-type-safety-and-configuration-parity
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - docs/configuration-reference.md
  - docs/api-reference.md
  - docs/configuration-schema.json
  - samples/minimal-config.json
  - samples/development.json
  - samples/v2-with-bonding.json
  - samples/v3-authenticated-control.json
  - __tests__/config-docs-parity.test.js
autonomous: true
requirements:
  - V1-UI-001
  - V1-UI-002
user_setup: []
must_haves:
  truths:
    - "Phase 4 public docs, docs schema, and samples must reflect the same config contract as source."
    - "Decision coverage: D-06, D-08, D-09, D-13, D-14, D-15, D-16, and D-17."
    - "Docs and samples are parity targets, not a broad rewrite target."
    - "Samples stay minimal and realistic; do not add every optional field to every sample."
    - "Phase completion runs focused docs/sample tests and the broad lint/type/build/Jest gate."
  artifacts:
    - path: docs/configuration-schema.json
      provides: "Published JSON schema parity"
      contains: "udpMetaPort"
    - path: docs/configuration-reference.md
      provides: "Operator configuration field reference"
      contains: "udpMetaPort"
    - path: __tests__/config-docs-parity.test.js
      provides: "Docs schema and sample compatibility coverage"
      contains: "samples"
  key_links:
    - from: .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-CONTEXT.md
      to: docs/configuration-reference.md
      via: "D-13 and D-14 require docs/sample parity without artificial sample churn"
      pattern: "udpMetaPort"
    - from: src/connection-config.ts
      to: __tests__/config-docs-parity.test.js
      via: "Runtime validation is the acceptance oracle for sample files"
      pattern: "validateConnectionConfig"
---

<objective>
Update public configuration docs, docs schema, and samples after schema/runtime parity changes, then run phase-level validation.

Purpose: Ensure operators see the same configuration contract that the shared schema and runtime validator enforce.
Output: Updated docs/schema artifacts, a docs/sample parity test, and a completed Phase 4 validation gate.
</objective>

<execution_context>
@C:/Users/karl-/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/karl-/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-CONTEXT.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-RESEARCH.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-VALIDATION.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-PATTERNS.md
@src/shared/connection-schema.ts
@src/connection-config.ts
@docs/configuration-reference.md
@docs/api-reference.md
@docs/configuration-schema.json
@samples/minimal-config.json
@samples/development.json
@samples/v2-with-bonding.json
@samples/v3-authenticated-control.json
</context>

<threat_model>

## Threat Model

| ID     | Threat                                                                                      | Severity | Mitigation                                                                                                                                 |
| ------ | ------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-4-08 | Operator docs omit newly public `udpMetaPort` or describe behavior differently from runtime | high     | Update config reference, API mutable-field lists, docs schema, and tests to name `udpMetaPort` with the same optional v1 metadata meaning. |
| T-4-09 | Samples include invalid or artificial config fields that fail runtime validation            | high     | Add a docs/sample parity test that parses each sample and validates every connection with `validateConnectionConfig()`.                    |
| T-4-10 | Docs examples expose secret values or imply per-connection management tokens                | high     | Keep management token examples top-level only and use placeholder language, not real tokens.                                               |
| T-4-11 | Broad validation failures caused by earlier plans are missed                                | medium   | Run the full lint, backend typecheck, webapp typecheck, build, and Jest gate before Phase 4 completion.                                    |

Block condition: any high-severity threat without docs/test mitigation blocks plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update public docs and configuration schema artifact</name>
  <files>docs/configuration-reference.md, docs/api-reference.md, docs/configuration-schema.json</files>
  <read_first>docs/configuration-reference.md, docs/api-reference.md, docs/configuration-schema.json, src/shared/connection-schema.ts, src/connection-config.ts, src/index.ts</read_first>
  <action>Update `docs/configuration-reference.md` so the connection field reference includes `udpMetaPort` as an optional number with valid range `1024 - 65535` and usage `Optional separate UDP port for v1 metadata packets; ignored by v2/v3`. Keep the existing v1 metadata caveat text. Update `docs/api-reference.md` connection create/update field lists so `udpMetaPort` appears with other optional connection fields and `managementApiToken` remains documented only as top-level plugin/management auth configuration. Update `docs/configuration-schema.json` so top-level `properties` includes `managementApiToken` as a string and `requireManagementApiToken` as a boolean with default false, and the connection definition includes `udpMetaPort` with `type: "number"`, `minimum: 1024`, and `maximum: 65535`. Do not add fake secret values to examples.</action>
  <verify>Run `node -e "JSON.parse(require('fs').readFileSync('docs/configuration-schema.json','utf8')); console.log('schema ok')"` and `rg -n "udpMetaPort|managementApiToken|requireManagementApiToken" docs/configuration-reference.md docs/api-reference.md docs/configuration-schema.json`.</verify>
  <acceptance_criteria>
    - `docs/configuration-reference.md` contains `udpMetaPort`.
    - `docs/api-reference.md` contains `udpMetaPort` in connection create/update field guidance.
    - `docs/configuration-schema.json` parses as JSON.
    - `docs/configuration-schema.json` contains `managementApiToken`, `requireManagementApiToken`, and `udpMetaPort`.
    - No docs example adds a concrete production-looking management token value.
  </acceptance_criteria>
  <done>Public docs and docs schema describe the same public config fields as source.</done>
</task>

<task type="auto">
  <name>Task 2: Add docs and sample parity tests</name>
  <files>__tests__/config-docs-parity.test.js, samples/minimal-config.json, samples/development.json, samples/v2-with-bonding.json, samples/v3-authenticated-control.json</files>
  <read_first>__tests__/connection-config.test.js, __tests__/schema-compat.test.js, src/connection-config.ts, docs/configuration-schema.json, samples/minimal-config.json, samples/development.json, samples/v2-with-bonding.json, samples/v3-authenticated-control.json</read_first>
  <action>Create `__tests__/config-docs-parity.test.js`. In it, read `docs/configuration-schema.json` with `fs.readFileSync()` and `JSON.parse()`, then assert `schema.properties.managementApiToken`, `schema.properties.requireManagementApiToken`, and `schema.definitions.connection.properties.udpMetaPort` exist. Read each sample JSON file from `samples/minimal-config.json`, `samples/development.json`, `samples/v2-with-bonding.json`, and `samples/v3-authenticated-control.json`; assert each has a non-empty `connections` array and every connection returns `null` from `validateConnectionConfig(connection, "connections[${index}].")`. Only edit sample files if this test reveals an existing sample is invalid; keep samples realistic and do not add `udpMetaPort` unless a sample intentionally demonstrates v1 metadata.</action>
  <verify>Run `npm.cmd test -- --runTestsByPath __tests__/config-docs-parity.test.js` and `rg -n "configuration-schema.json|minimal-config.json|validateConnectionConfig|udpMetaPort" __tests__/config-docs-parity.test.js`.</verify>
  <acceptance_criteria>
    - `__tests__/config-docs-parity.test.js` exists.
    - The test reads `docs/configuration-schema.json`.
    - The test reads all four sample JSON files.
    - The test calls `validateConnectionConfig`.
    - The focused docs/sample parity test exits 0.
  </acceptance_criteria>
  <done>Docs schema and samples are protected by automated parity coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 4 focused and broad validation</name>
  <files>tsconfig.webapp.json, src/webapp/components/PluginConfigurationPanel.tsx, src/shared/connection-schema.ts, src/connection-config.ts, src/types.ts, src/routes/config.ts, src/routes/connections.ts, docs/configuration-reference.md, docs/api-reference.md, docs/configuration-schema.json, __tests__/PluginConfigurationPanel.test.js, __tests__/webapp.test.js, __tests__/schema-compat.test.js, __tests__/connection-config.test.js, __tests__/routes.config-validation.test.js, __tests__/config-docs-parity.test.js</files>
  <read_first>package.json, tsconfig.json, tsconfig.webapp.json, .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-VALIDATION.md</read_first>
  <action>Run the Phase 4 focused commands, then the broad gate. Focused commands: `npx.cmd tsc -p tsconfig.webapp.json --noEmit --noImplicitAny true`; `npm.cmd test -- --runTestsByPath __tests__/PluginConfigurationPanel.test.js __tests__/webapp.test.js`; `npm.cmd test -- --runTestsByPath __tests__/schema-compat.test.js __tests__/connection-config.test.js __tests__/routes.config-validation.test.js`; `npm.cmd test -- --runTestsByPath __tests__/config-docs-parity.test.js`. Broad gate: `npm.cmd run lint`; `npm.cmd run check:ts`; `npx.cmd tsc -p tsconfig.webapp.json --noEmit`; `npm.cmd run build`; `npm.cmd test`. Fix only failures caused by Phase 4 changes. If a broad command cannot be run locally, record the exact failure in the plan summary.</action>
  <verify>Run every command listed in the action.</verify>
  <acceptance_criteria>
    - Webapp noImplicitAny typecheck exits 0.
    - Focused component/helper tests exit 0.
    - Focused schema/config/route tests exit 0.
    - Docs/sample parity test exits 0.
    - Broad lint, backend typecheck, webapp typecheck, build, and full Jest gates exit 0, or any unavailable command is documented with exact failure text in `04-03-SUMMARY.md`.
  </acceptance_criteria>
  <done>Phase 4 is ready for verification with focused and broad validation evidence.</done>
</task>

</tasks>

<verification>

- [ ] `node -e "JSON.parse(require('fs').readFileSync('docs/configuration-schema.json','utf8')); console.log('schema ok')"` prints `schema ok`.
- [ ] `npm.cmd test -- --runTestsByPath __tests__/config-docs-parity.test.js` passes.
- [ ] `npx.cmd tsc -p tsconfig.webapp.json --noEmit --noImplicitAny true` passes.
- [ ] `npm.cmd test -- --runTestsByPath __tests__/PluginConfigurationPanel.test.js __tests__/webapp.test.js` passes.
- [ ] `npm.cmd test -- --runTestsByPath __tests__/schema-compat.test.js __tests__/connection-config.test.js __tests__/routes.config-validation.test.js` passes.
- [ ] `npm.cmd run lint` passes.
- [ ] `npm.cmd run check:ts` passes.
- [ ] `npx.cmd tsc -p tsconfig.webapp.json --noEmit` passes.
- [ ] `npm.cmd run build` passes.
- [ ] `npm.cmd test` passes.
- [ ] `npx.cmd prettier --check docs/configuration-reference.md docs/api-reference.md docs/configuration-schema.json samples/minimal-config.json samples/development.json samples/v2-with-bonding.json samples/v3-authenticated-control.json __tests__/config-docs-parity.test.js` passes.

</verification>

<success_criteria>

- V1-UI-001 and V1-UI-002 are covered by focused validation and the broad repository gate.
- Public docs, docs schema, and samples agree with shared schema and runtime validation.
- `udpMetaPort` is documented as optional v1 metadata configuration.
- Management token configuration remains top-level and secret-safe.
- No unrelated source refactor, protocol redesign, package-lock change, generated tarball, or sample bloat is introduced.

</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-03-SUMMARY.md`.
</output>
