---
phase: 04-schema-ui-type-safety-and-configuration-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.webapp.json
  - src/webapp/components/PluginConfigurationPanel.tsx
  - __tests__/PluginConfigurationPanel.test.js
  - __tests__/webapp.test.js
autonomous: true
requirements:
  - V1-UI-001
  - V1-UI-002
user_setup: []
must_haves:
  truths:
    - "Phase 4 tightens the webapp configuration surface without changing the operator workflow."
    - "Decision coverage: D-01, D-02, D-03, D-04, D-10, D-11, D-12, D-15, D-16, and D-17."
    - "The current baseline passes `npx.cmd tsc -p tsconfig.webapp.json --noEmit --noImplicitAny true`; enabling `noImplicitAny` is allowed if focused tests remain green."
    - "RJSF form changes must preserve frontend `_id`, persistent `connectionId`, dirty-state behavior, management token behavior, and save payload shape."
    - "Do not rewrite `src/webapp/index.ts` or attempt full webapp `strict: true` unless it remains incidental and low-risk."
  artifacts:
    - path: tsconfig.webapp.json
      provides: "Webapp typecheck tightening"
      contains: '"noImplicitAny": true'
    - path: src/webapp/components/PluginConfigurationPanel.tsx
      provides: "Typed configuration-panel form event and save payload seam"
      contains: "handleFormChange"
    - path: __tests__/PluginConfigurationPanel.test.js
      provides: "Component coverage for dirty state, RJSF change events, and save payload parity"
      contains: "RJSF"
  key_links:
    - from: .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-CONTEXT.md
      to: src/webapp/components/PluginConfigurationPanel.tsx
      via: "D-01 through D-04 require incremental type tightening around the config UI"
      pattern: "handleFormChange"
    - from: .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-UI-SPEC.md
      to: src/webapp/components/PluginConfigurationPanel.tsx
      via: "UI contract preserves visible copy and interactions"
      pattern: "Save Configuration"
---

<objective>
Tighten the webapp configuration-panel type boundary and add focused UI flow coverage.

Purpose: Close the highest-value TypeScript gaps around RJSF config editing while preserving current operator behavior.
Output: `tsconfig.webapp.json` with `noImplicitAny` enabled, a typed `PluginConfigurationPanel.tsx` form-change path, and focused component/helper tests proving dirty-state and save-payload behavior did not regress.
</objective>

<execution_context>
@C:/Users/karl-/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/karl-/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-CONTEXT.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-RESEARCH.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-VALIDATION.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-PATTERNS.md
@.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-UI-SPEC.md
@tsconfig.webapp.json
@src/webapp/components/PluginConfigurationPanel.tsx
@src/shared/connection-schema.ts
@__tests__/PluginConfigurationPanel.test.js
@__tests__/webapp.test.js
</context>

<threat_model>

## Threat Model

| ID     | Threat                                                                                                           | Severity | Mitigation                                                                                                                                                                      |
| ------ | ---------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4-01 | Type tightening causes broad unrelated webapp churn or masks explicit `any` with weaker casts                    | high     | Enable `noImplicitAny` only after the baseline command passes; type the configuration-panel RJSF event with a local event shape and avoid broad `src/webapp/index.ts` rewrites. |
| T-4-02 | UI tests or type changes alter persisted connection payloads, dirty-state behavior, or management-token behavior | high     | Add component tests for no-op RJSF changes, mode switches, `_id` stripping, `connectionId` preservation, and management token save behavior.                                    |
| T-4-03 | RJSF mock changes make tests depend on implementation details instead of panel behavior                          | medium   | Keep the mock lightweight; expose `formData`, `schema`, and `onChange` only enough to drive public panel behavior.                                                              |

Block condition: any high-severity threat without tests and passing webapp typecheck blocks plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enable focused webapp noImplicitAny checking</name>
  <files>tsconfig.webapp.json</files>
  <read_first>tsconfig.webapp.json, package.json, .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-RESEARCH.md</read_first>
  <action>Set `compilerOptions.noImplicitAny` from `false` to `true` in `tsconfig.webapp.json`. Keep `compilerOptions.strict` as `false` and keep `strictNullChecks` as `true`. Do not change the include/exclude list or emit settings.</action>
  <verify>Run `npx.cmd tsc -p tsconfig.webapp.json --noEmit --noImplicitAny true` and `npx.cmd tsc -p tsconfig.webapp.json --noEmit`.</verify>
  <acceptance_criteria>
    - `tsconfig.webapp.json` contains `"noImplicitAny": true`.
    - `tsconfig.webapp.json` still contains `"strict": false`.
    - `npx.cmd tsc -p tsconfig.webapp.json --noEmit --noImplicitAny true` exits 0.
    - `npx.cmd tsc -p tsconfig.webapp.json --noEmit` exits 0.
  </acceptance_criteria>
  <done>Webapp typecheck rejects new implicit-any regressions while preserving the current non-strict migration boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Type the configuration-panel RJSF form-change seam</name>
  <files>src/webapp/components/PluginConfigurationPanel.tsx</files>
  <read_first>src/webapp/components/PluginConfigurationPanel.tsx, src/shared/connection-schema.ts, .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-PATTERNS.md, .planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-UI-SPEC.md</read_first>
  <action>Replace `function handleFormChange(e: any)` with a typed local event shape such as `type ConnectionFormChangeEvent = { formData?: Partial<ConnectionData> & Record<string, unknown> }`. In `handleFormChange`, read `const next = e.formData` and return early if `next` is missing. Build `proposed` and `merged` values as `ConnectionData` while preserving `conn._id`, preserving `connectionId` as `next.connectionId || conn.connectionId || conn._id`, and preserving `SHARED_FIELDS` copying. Keep visible copy from `04-UI-SPEC.md`: `Save Configuration`, `Save Changes`, `+ Add Server`, `+ Add Client`, and the duplicate-port warning text unchanged.</action>
  <verify>Run `rg -n "handleFormChange\\(e: any\\)|ConnectionFormChangeEvent|Save Configuration|Save Changes|\\+ Add Server|\\+ Add Client" src/webapp/components/PluginConfigurationPanel.tsx` and the webapp typecheck from Task 1.</verify>
  <acceptance_criteria>
    - `src/webapp/components/PluginConfigurationPanel.tsx` does not contain `handleFormChange(e: any)`.
    - `src/webapp/components/PluginConfigurationPanel.tsx` contains `ConnectionFormChangeEvent` or an equivalent typed event alias.
    - The mode-switch path still sets `_id: conn._id`.
    - The normal change path still sets `connectionId: next.connectionId || conn.connectionId || conn._id`.
    - The strings `Save Configuration`, `Save Changes`, `+ Add Server`, and `+ Add Client` remain present.
  </acceptance_criteria>
  <done>The RJSF boundary is typed without changing the operator-facing configuration workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen component tests for RJSF change and save parity</name>
  <files>__tests__/PluginConfigurationPanel.test.js, __tests__/webapp.test.js</files>
  <read_first>__tests__/PluginConfigurationPanel.test.js, __tests__/webapp.test.js, src/webapp/components/PluginConfigurationPanel.tsx, src/shared/connection-schema.ts</read_first>
  <action>Extend the `@rjsf/core` mock in `__tests__/PluginConfigurationPanel.test.js` so tests can invoke the most recent form `onChange` with an explicit `{ formData }` object and inspect the schema/formData passed to the mock. Add tests named `does not mark dirty when RJSF emits unchanged form data` and `preserves connection identity when switching mode through RJSF`. The unchanged-form test must render `ONE_SERVER`, invoke the first mocked form's `onChange` with the same formData already rendered, and assert `screen.queryByText("You have unsaved changes.")` is null. The mode-switch test must invoke `onChange` with `serverType: "client"`, save, inspect the POST body, and assert `_id` is absent, `connectionId` is present, and shared fields `name`, `udpPort`, `secretKey`, `stretchAsciiKey`, `useMsgpack`, `usePathDictionary`, and `protocolVersion` are preserved. If `__tests__/webapp.test.js` has duplicated schema-default logic that can be replaced by imported helpers without broad churn, add one focused helper test that imports `buildWebappConnectionSchema()` and proves v2 client schemas include `reliability`, `congestionControl`, `bonding`, and `alertThresholds`.</action>
  <verify>Run `npm.cmd test -- --runTestsByPath __tests__/PluginConfigurationPanel.test.js __tests__/webapp.test.js` and `rg -n "does not mark dirty when RJSF emits unchanged form data|preserves connection identity when switching mode through RJSF|buildWebappConnectionSchema" __tests__/PluginConfigurationPanel.test.js __tests__/webapp.test.js`.</verify>
  <acceptance_criteria>
    - `__tests__/PluginConfigurationPanel.test.js` contains a test named `does not mark dirty when RJSF emits unchanged form data`.
    - `__tests__/PluginConfigurationPanel.test.js` contains a test named `preserves connection identity when switching mode through RJSF`.
    - The save-payload assertions prove `_id` is not serialized and `connectionId` is serialized.
    - The focused component/helper Jest command exits 0.
  </acceptance_criteria>
  <done>Configuration UI flow behavior is covered before schema parity changes build on it.</done>
</task>

</tasks>

<verification>

- [ ] `npx.cmd tsc -p tsconfig.webapp.json --noEmit --noImplicitAny true` passes.
- [ ] `npx.cmd tsc -p tsconfig.webapp.json --noEmit` passes.
- [ ] `npm.cmd test -- --runTestsByPath __tests__/PluginConfigurationPanel.test.js __tests__/webapp.test.js` passes.
- [ ] `rg -n "handleFormChange\\(e: any\\)" src/webapp/components/PluginConfigurationPanel.tsx` returns no matches.
- [ ] `rg -n "does not mark dirty when RJSF emits unchanged form data|preserves connection identity when switching mode through RJSF" __tests__/PluginConfigurationPanel.test.js` shows the new tests.
- [ ] `npx.cmd prettier --check tsconfig.webapp.json src/webapp/components/PluginConfigurationPanel.tsx __tests__/PluginConfigurationPanel.test.js __tests__/webapp.test.js` passes.

</verification>

<success_criteria>

- V1-UI-001 is advanced by a stricter webapp typecheck boundary and a typed config-panel RJSF event path.
- V1-UI-002 is protected by component tests that preserve save-payload and connection identity behavior.
- The existing operator UI copy, management token behavior, dirty-state behavior, and add/remove connection workflow are unchanged.
- No unrelated legacy dashboard rewrite, new UI library, new runtime dependency, package-lock change, or generated tarball is introduced.

</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-ui-type-safety-and-configuration-parity/04-01-SUMMARY.md`.
</output>
