// Re-export shim — implementation re-homed to the L0 foundation layer during
// the productization rewrite (docs/planning/rewrite, doc 05). Old import paths
// keep working until the cutover phase deletes these shims.
export * from "./foundation/constants";
